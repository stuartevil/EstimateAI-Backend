"""Auth middleware placeholder — JWT validation handled via FastAPI dependencies."""

# Bearer token auth is implemented in app.core.dependencies.
# This module is reserved for future API-key or session middleware.
